<% provide(:title, 'All publications') %>
<h1>All publications</h1>

<ul class="publications">
  <% @publications.each do |publication| %>
    <li>
      <%=  publication.titre.concat(" || ").concat(publication.nom).concat(" || ").concat(publication.annee) %>
    </li>
  <% end %>
</ul>

