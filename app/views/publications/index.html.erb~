<% provide(:title, 'All publications') %>
<h1>All publications</h1>

<ul class="publications">
  <% @publications.each do |publication| %>
    <li>
      <%=  publication.titre.concat(" || ").concat(publication.typePublication).concat(" || ").concat(publication.nom).concat(" || ").concat(publication.annee).concat(" || ").concat(publication.mois).concat(" || ").concat(publication.pages).concat(" || ").concat(publication.notes).concat(" || ").concat(publication.resume) %>
    </li>
  <% end %>
</ul>

